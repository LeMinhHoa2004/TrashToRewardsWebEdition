@model TrashToRewardsV2.Models.THONGBAONVGQ

@{
    ViewData["Title"] = "Chỉnh sửa thông báo Nhân viên giao quà";
}

<div class="container py-4">
    <div class="bg-white rounded p-4 shadow-sm mb-4">
        <h2 class="fw-bold text-success mb-4">Chỉnh Sửa Thông Báo Nhân Viên Giao Quà</h2>

        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card border-0 shadow-sm">
                    <div class="card-body p-4">
                        <form asp-action="EditNVGQ" method="post">
                            @Html.AntiForgeryToken()
                            @if (!ViewData.ModelState.IsValid)
                            {
                                <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
                            }

                            <input type="hidden" asp-for="MaTBNVGQ" />

                            <div class="mb-4">
                                <label asp-for="MaNVGQ" class="form-label fw-medium">Nhân viên giao quà</label>
                                <select asp-for="MaNVGQ" asp-items="ViewBag.NhanVienGQList" class="form-select"></select>
                            </div>

                            <div class="mb-4">
                                <label asp-for="NoiDungNVGQ" class="form-label fw-medium">Nội dung thông báo</label>
                                <textarea asp-for="NoiDungNVGQ" class="form-control" rows="5" placeholder="Nhập nội dung thông báo..."></textarea>
                            </div>

                            <div class="d-flex gap-2">
                                <button type="submit" class="btn btn-success px-4">
                                    <i class="bi bi-check-circle me-2"></i>Lưu thay đổi
                                </button>
                                <a asp-action="ThongBaoNVGQ" class="btn btn-outline-secondary">
                                    <i class="bi bi-arrow-left me-2"></i>Quay lại
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>