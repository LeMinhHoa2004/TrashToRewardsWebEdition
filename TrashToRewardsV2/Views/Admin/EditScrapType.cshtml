@model TrashToRewardsV2.Models.LOAIPHELIEU

@{
    ViewData["Title"] = "Chỉnh sửa loại phế liệu";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card border-0 shadow-lg">
                <div class="card-body p-lg-5 p-4">
                    <h2 class="fw-bold text-success mb-4">✏️ Chỉnh sửa loại phế liệu</h2>

                    <form asp-action="EditScrapType" method="post">
                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
                        }
                        <input type="hidden" asp-for="MaLoaiPL" />

                        <div class="mb-4">
                            <label asp-for="TenLoai" class="form-label fw-medium">Tên loại</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0">
                                    <i class="bi bi-tag-fill text-success"></i>
                                </span>
                                <input asp-for="TenLoai" class="form-control border-0 bg-light" />
                            </div>
                            <span asp-validation-for="TenLoai" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="DonVi" class="form-label fw-medium">Đơn vị</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0">
                                    <i class="bi bi-calculator text-success"></i>
                                </span>
                                <input asp-for="DonVi" class="form-control border-0 bg-light" />
                            </div>
                            <span asp-validation-for="DonVi" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="TiLeDiem" class="form-label fw-medium">Tỉ lệ điểm</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0">
                                    <i class="bi bi-percent text-success"></i>
                                </span>
                                <input asp-for="TiLeDiem" class="form-control border-0 bg-light" />
                            </div>
                            <span asp-validation-for="TiLeDiem" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-3 mt-4">
                            <button type="submit" class="btn btn-success px-4 py-2 rounded-pill fw-medium">
                                <i class="bi bi-check-circle me-2"></i>Lưu thay đổi
                            </button>
                            <a asp-action="Dashboard" class="btn btn-outline-secondary px-4 py-2 rounded-pill fw-medium">
                                <i class="bi bi-arrow-left me-2"></i>Quay lại
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}