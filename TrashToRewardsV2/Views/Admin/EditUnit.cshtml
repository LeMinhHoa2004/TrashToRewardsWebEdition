@model TrashToRewardsV2.Models.DONVITHUGOM

@{
    ViewData["Title"] = "Chỉnh sửa đơn vị thu gom";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card border-0 shadow-lg">
                <div class="card-body p-lg-5 p-4">
                    <h2 class="fw-bold text-success mb-4">✏️ Chỉnh sửa đơn vị thu gom</h2>

                    <form asp-action="EditUnit" method="post">
                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
                        }
                        <input type="hidden" asp-for="MaDV" />

                        <div class="mb-4">
                            <label asp-for="TenDV" class="form-label fw-medium">Tên đơn vị</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0">
                                    <i class="bi bi-building-fill text-success"></i>
                                </span>
                                <input asp-for="TenDV" class="form-control border-0 bg-light" />
                            </div>
                            <span asp-validation-for="TenDV" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="DiaChiDV" class="form-label fw-medium">Địa chỉ</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0">
                                    <i class="bi bi-geo-alt-fill text-success"></i>
                                </span>
                                <input asp-for="DiaChiDV" class="form-control border-0 bg-light" />
                            </div>
                            <span asp-validation-for="DiaChiDV" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="SDTDV" class="form-label fw-medium">Số điện thoại</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0">
                                    <i class="bi bi-telephone-fill text-success"></i>
                                </span>
                                <input asp-for="SDTDV" class="form-control border-0 bg-light" />
                            </div>
                            <span asp-validation-for="SDTDV" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-3 mt-4">
                            <button type="submit" class="btn btn-success px-4 py-2 rounded-pill fw-medium">
                                <i class="bi bi-check-circle me-2"></i>Cập nhật
                            </button>
                            <a asp-action="Dashboard" class="btn btn-outline-secondary px-4 py-2 rounded-pill fw-medium">
                                <i class="bi bi-arrow-left me-2"></i>Quay lại
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}